var patterns = require('./conf/patterns');

var check = funciton(){
	for (var i = fusker.patterns.sql.length - 1; i >= 0; --i) {
	    if (fusker.patterns.sql[i].test(req.url)) {
	      fusker.http.handleAttack('SQLi-' + i, req, res);
	      return;
	    }
	}
};

exports.check = check;