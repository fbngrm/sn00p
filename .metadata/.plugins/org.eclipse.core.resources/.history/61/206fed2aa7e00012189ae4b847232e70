
var Router = function(){
	
	// when invalid client deny the response
	this.deny = function(response, msg) {
		response.writeHead(403);
		response.write(msg);
		response.end();
	}
}

exports.Router = Router;