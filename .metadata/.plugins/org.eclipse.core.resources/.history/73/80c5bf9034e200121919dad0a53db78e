var patterns = require('./conf/patterns').patterns;

var check = funciton(){
	for (var i = patterns.sql.length - 1; i >= 0; --i) {
	    if (patterns.sql[i].test(req.url)) {
	      fusker.http.handleAttack('SQLi-' + i, req, res);
	      return;
	    }
	}
};

exports.check = check;