var patterns = require('./conf/patterns').patterns;


var SQLi = function(){
	var check = function(){
		for (var i = patterns.sql.length -1; i >= 0; --i) {
			if (patterns.sql[i].test(req.url)) return true;
		}
		return false;
	};
};

exports.SQLi = SQLi;